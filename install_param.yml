# Version numbers
ghc_version: "9.6.7"
cabal_version: "3.12.1.0"
user_home: "/home/danu"

# Log file for shell command output
log_file: "{{ user_home }}/ansible-install.log"

# Project root for source builds
project_home: "{{ user_home }}/git"
